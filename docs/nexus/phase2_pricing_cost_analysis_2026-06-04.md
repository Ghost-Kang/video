# Phase 2 配额付费成本核算 — 2026-06-04

**作者**: Claude(phase2「按顺序做 8 件事」step 6)
**性质**: 用 `cost_guard.py` 的真实预测单价,算单条闭环成本 → 给 freemium/Pro 分层 + 定价建议。**最终拍板 founder。**
**口径**: 下面是 cost_guard 的**预测价**(用于 enqueue 前预算 + 超额拦截);实际结算可能小幅偏离,但这是规划该用的数。

---

## 1. 单件成本(`cost_guard.py` 预测价)

| 动作 | 单价 (¥) | 来源常量 | 说明 |
|---|---:|---|---|
| 爆点分析 | **1.20** | `PREDICT_ANALYSIS_CNY` | ~1min 视频(剧情故事线分析 ¥1/min + ARK ~0.1)。需转写再 +0.30 |
| 改写(代笔) | **1.00** | `PREDICT_REWRITE_CNY` | doubao,24h 缓存 |
| 草稿图 | **1.50/张** | `PREDICT_SHOT_IMAGE_CNY` | Seedream,一条 3–5 张 |
| 图生视频 | **0.30/秒** | `PREDICT_VIDEO_SECOND_CNY` | Seedance,典型 5s/镜 |
| 追问 Q&A | 0.05/次 | `PREDICT_ASK_CNY` | cascade_ask |
| 转写 | 0.30 | `PREDICT_TRANSCRIBE_CNY` | 按需 |
| 合成成片 | **0** | — | ffmpeg 本地,免费 |

caps:`CASCADE_RUN_CAP_CNY=3.0`(单 run)、`CASCADE_USER_DAY_CAP_CNY=30.0`(单用户/天)。

## 2. 单条闭环成本(按深度)

| 闭环深度 | 成本 (¥) |
|---|---:|
| 只分析 | 1.20 |
| 分析 + 改写(脚本 + 发布包) | 2.20 |
| + 3 张草稿图 | 6.70 |
| + 成片(3 镜 × 5s) | **11.20** |
| + 成片(5 镜 × 5s) | **18.20** |

> 缓存红利:分析是**永久缓存**(重复 URL = ¥0);改写 24h 缓存。真实人均成本比上表低(命中缓存)。

## 3. 三个关键洞察

1. **生成(图+视频)是成本黑洞** —— 占一条成片的 **80%+**:¥9–16 of ¥11–18。分析+改写才 ¥2.2。**钱花在生成,不花在拆解。**
2. **¥39/月 撑不起「无限生成」** —— 3 条成片 ≈ ¥33–55 成本 > ¥39 收入。**所以 Pro 必须给生成设额度,不能全放开**,否则重度用户直接亏穿。
3. **🔴 成本守卫在 prod 实际全失效(比"退化"更糟)** —— 后续 16-agent 审计坐实:生成工具根本不 emit `generation_cost` 事件(只 emit `SHOT_*_RETURNED`),而 `sum_generation_cost` 只数 `generation_cost` → prod 实测该事件 = **0** → **连 ¥30/天都在读空表**,成本既没拦也没记录(看不到)。完整 root(run_id=None + 事件类型错配 + 后台任务脱 RUN_CTX)+ 「改错全挂」的修法见 [`phase2_bugclass_audit_2026-06-04.md`](phase2_bugclass_audit_2026-06-04.md) #1-7。下面这条是其中一环:

   **per-run ¥3 cap 退化** —— `agent_runner.py:330/464` 把 run ctx 的 `run_id` 恒设 None,于是 `cost_guard` 的 `_run_cost(None)≈0`,**单 run ¥3 上限形同虚设**(这也正解释了为什么生成能真跑通,而非被 ¥3.70>¥3 卡死)。**真正生效的护栏只剩 `¥30/用户/天`**(`CASCADE_USER_DAY_CAP_CNY`)。含义:① 成本控制比设计的粗(只有日级,没有 per-条/per-run 粒度);② 30 人 Beta 最坏敞口 = 30 × ¥30 = **¥900/天 ≈ ¥2.7w/月**(全员每天打满)。配额付费落地时建议把 per-run 粒度修回来(run_id 真正按 turn 生成),否则免费层的「N/天」配额没有 run 级成本闸去兜底,只能靠日上限。

## 4. 推荐分层(数字待 founder 定)

**核心思路:免费/Pro 的差别主要在「便宜的核心(分析+改写)」放开程度;「贵的生成」一律有额度 + 超额计费。**

| | Free ¥0 | Pro ¥39/月 |
|---|---|---|
| 定位 | 卖「拆解+改写」核心价值(便宜、感知高) | 核心放开 + 生成有额度 |
| 分析 | ~5/天(滚动) | ~30/天(防滥用上限) |
| 改写 | ~2/天 | ~15/天 |
| 草稿图 | 1 张试用 | included 生成额度 ≈ **¥30**(≈ 4–5 张图 or 1–2 条短成片) |
| 成片 | ✗(看不到生成,只看拆解+脚本) | 含额度内,超额按 credit |
| 超额 | 升 Pro | ¥1.5/图、¥0.3/秒(或打包 credit 包) |

**毛利**:Free 人均成本敞口 ~¥8/天上限(实际因缓存更低);Pro ¥39 在「核心无限 + ¥30 生成额度」下接近打平,**利润靠超额 credit + 不满额用户**。

## 5. 要 founder 拍板的数字(❓7 收口)

1. Free 日配额具体数(分析 5/改写 2?)。
2. Pro included 生成额度(¥30?还是「含 N 条成片/月」更好懂)。
3. 超额计费:做 credit/按量(工程量大),还是先简单「Pro 含 N 条/月,超了排队到下月」?
4. 是否要在 Pro 里把视频成片单列(它是唯一会亏穿的一项)。
5. 配额付费工程化时,要不要顺手修「per-run cap 退化」(run_id 恒 None)—— 让 run 级成本闸真正生效,给免费层「N/天」配额兜底。

> 工程侧:配额/计费系统(quota tracking + credit 扣减 + 超额 banner)是 P3 的活,依赖本核算定下数字。当前唯一生效的硬护栏是 `CASCADE_USER_DAY_CAP_CNY=30`(一行 env 可调);per-run ¥3 cap 因 run_id=None 失效(见洞察 3)。
